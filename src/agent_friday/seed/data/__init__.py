"""Bundled data schemas for Agent Friday's career pipeline.

Ships as real package data inside the wheel/sdist (agent_friday.seed.data),
not a repo-root-only convenience — see agent_friday/seed/__init__.py for the
history of why this had to move.
"""
