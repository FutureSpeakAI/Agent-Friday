# Seam coverage

Legend: UNSTARTED / IN PROGRESS / SWEEP 1 CLEAN / SWEEP 2 CLEAN (done) / PARKED

## STATUS SYNC (2026-09-04, afternoon) — read this before trusting anything below

This file was last substantively written at 08:33 and had gone stale by
several hours while findings.jsonl and progress.md kept updating —
Stephen flagged this directly: "the one artifact that says what you
haven't looked at... is the one where staleness misleads most."
Synchronized now against findings.jsonl's actual current status.

**What changed underneath this table, and what didn't:** Stephen
delegated the entire queued-decision backlog to Claude on 2026-09-04.
Of every finding this table names as "queued" below, **all but a
handful are now FIXED** (checked directly against findings.jsonl as of
this sync — see the per-row `[SYNC: ...]` tags added below). The
**sweep-count language is UNCHANGED and still means exactly what it
always did** — "SWEEP N FOUND NEW... needs an (N+1)th clean sweep" is
about fresh-context verification passes, which have NOT happened since
this table was last written; fixing a queued finding is not the same
claim as a clean sweep, and this sync does not claim any seam moved to
"CLOSED" that wasn't already there. Read a `[SYNC: ...]` tag as "this
specific named finding's disposition has changed"; read the surrounding
prose and sweep count as "still exactly as accurate/stale as it was
about what a fresh look would find."

Still genuinely open as of this sync (not fixed, not a misdiagnosis):
**F48** (in hand in a separate session, not re-escalated here), **Q6(a)**
and **Q10** (escalated to Stephen with a specific question each — see
progress.md's DELEGATION RESOLUTION and Round 9 sections), and the small
set of "observed, no action needed" items (Q12, Q13, Q15, F7) that were
reviewed and deliberately left as accepted, documented non-issues rather
than fixed or escalated — see each one's own findings.jsonl entry for why.

## Exit criteria (corrected 2026-09-04, per handoff from the session that
## wrote the gauntlet-loop prompt — see progress.md "Handoff" section)

The prompt's original wording ("two consecutive fresh-context sweeps ...
and every claim on it holds blind") cannot be satisfied for a seam carrying
a BROKEN finding whose fix is queued rather than landed — a queued fix
never "holds." The corrected exit condition, which this run uses:

A seam is done when **two consecutive fresh-context sweeps surface nothing
new**, AND **every claim on it carries a verdict with an artifact**:
- HOLDS — the observation/call-graph that established it (findings.jsonl `H*` entries), or
- BROKEN/UNREACHED — a red probe under tests/gauntlet/ pinning it (fixed: probe now green with red/green/red evidence; queued: probe stays red, evidence + reason in progress.md's QUEUED FOR STEPHEN section).

Green-everywhere is not the bar. Judged-everywhere is.

| Seam | Status | Notes |
|---|---|---|
| Settings write → settings read | SWEEP 6 FOUND NEW (F40: Federation panel's per-peer ask/allow/block trust control is completely fake — no backend route, no enforcement anywhere — queued) — needs a 7th clean sweep | Round 1: F3 (context_retention_days, queued+probe-pinned), F6 (camera_auto_describe, dead, low-priority), 7 HOLDS confirmed. Round 3: F24 (persona/distribution presets mostly placebo, queued). Round 4: confirmed General/Appearance dead toggles already tracked elsewhere — not re-reported. Found Q17 (off_record's "for this session" copy claim false). Round 5 (this sweep): walked every content-policy/minor-mode DEFAULT_SETTINGS key looking for F29's exact defect class recurring — clean, F29 remains the only child-safety gap (music_engine's separate, documented harm floor is a design decision, not a contradicted claim). Work tab and Economy tab checked fresh, no new defects (Work tab's claim already covered by Q18). But found F40 in the Federation panel. **[SYNC 2026-09-04: F3, F6, F24, Q17, F40 all now FIXED per the 2026-09-04 delegation — see findings.jsonl. Sweep count/"needs a 7th" unchanged — no new fresh sweep has run.]** |
| Prompt assembly → provider payload | SWEEP 2 FOUND NEW (F35: _generate_text never checked the router's refuse/vault_access verdicts, unlike its sibling _generate_agent — FIXED) — needs a 3rd clean sweep | Round 2: KG retrieval, context_injection, web_fetch/firecrawl, /api/analyze, and routes/voice.py's Gemini Live gating all traced and HOLD. F12: egress_gate.py has zero handling of the OpenAI-compatible `tool_calls` wire field (vs. the already-fixed Anthropic `tool_use` equivalent) — structural gap confirmed, but the two obvious cross-call vectors are incidentally closed. Round 2b (this sweep): closed the long-open cross-provider retry question — every worker/orchestrator/sub-agent retry path rebuilds a fresh messages list rather than reusing a mutated one across a provider switch, confirmed clean. F31's and F33's new call sites both confirmed still routed through the egress gate correctly, no bypass introduced tonight. But found F35 while comparing _generate_text against its already-correct sibling — fixed. **[SYNC 2026-09-04: F12 also now FIXED (see findings.jsonl). Sweep count unchanged.]** |
| Router request → seat actually served | SWEEP 6 FOUND NEW (Q19: local_only/local_preferred's own "ALL turns"/"local first" promise is false for ordinary interactive tool-use chat — SEVERE, queued; Q20: task_overrides.voice is permanently inert, TaskType.VOICE unreachable, queued) — needs a 7th clean sweep | Round 1: F2 (provider_health gaps, FIXED), H2/H3/H6 confirmed HOLDS. Round 5: F30 (fallback ladder reuses gated-for-wrong-provider prompt, queued, systemic), Q12 (last_local_refusal race, latent/inert, observed). Round 6 (this sweep): _apply_cloud_provider substitution risk checked and clear (registry-first resolution + colon-first provider_family check catch every local-model shape reachable in practice); no second, inconsistent seat-resolution function found (model_seat_gate.resolve_local_seat is a documented no-op, not a competitor); no stale conversation_seat override survives a mid-conversation settings change (verified fresh-loaded every turn, plus _sync_capability_routing keeps the watched orchestrator_model key mirrored). But found Q19 (severe — see progress.md's top section) while checking _route_basic's other branches. **[SYNC 2026-09-04: Q19 and Q20 FIXED (Stephen's direct ruling, pre-delegation); F30 FIXED; F10 (named in the "UI copy ↔ code" row below) FIXED as a direct side effect of the Q19 fix, no separate code change. Q12 reviewed and left as documented, deliberately-accepted technical debt (no action needed — see findings.jsonl). Sweep count unchanged, no fresh sweep run today.]** |
| UI copy ↔ code (index.html / app.html labels vs behavior) | SWEEP 2 FOUND NEW (KG "local only = nothing ever leaves this machine" claim, FIXED via F34) — needs a 3rd clean sweep | Round 1 covered settings-panel toggles (F3/F5/F6/F7). Round 2 covered onboarding_copy.py (F10 — SEVERE, local_only's "nothing sent, ever" is false by default, queued as top priority; F11 — vault-passphrase copy overstatement, queued; H11 — 6 other claims confirmed accurate). Round 2b (this sweep, triggered by F31/F32 context): index.html/app.html's KG-settings copy ("Local only = nothing ever leaves this machine") was false the moment a cloud model was bound as the reasoning seat — same root cause as F34, fixed there (indexer._llm() now enforces the pin directly), so the copy is true again with no separate copy edit needed. MCP/Connectors panel copy and the settings-save toast path checked clean. Needs 2 more consecutive clean sweeps given how much this seam keeps finding. **[SYNC 2026-09-04: F10 FIXED (as a side effect of Q19's fix elsewhere, not a separate copy change); F11 FIXED. Sweep count unchanged.]** |
| index.html ↔ ui_parts/app.html divergence | SWEEP 2 FOUND NEW (F13 sharpened: SettingsTabIntelligence is a 5th high-severity gap, app.html's substitute tab still writes F7's deprecated flat keys — queued, probe updated) — needs a 3rd clean sweep | Round 1: F13 (stale "17" count corrected, real gap was 14 and growing, 4 high-severity components pinned+queued), F7 (orchestrator_model/creative_model flat-key divergence, HOLDS-with-latent-risk, not fixed — currently works). Round 2 (this sweep, systematic top-level-function diff reproducing the probe's own detection methodology): confirmed exactly 14 missing, 0 extra in app.html (reverse direction clean) — but found SettingsTabIntelligence (the permanently-visible Settings→Intelligence tab, central to tonight's F34 finding) and its 8 supporting sub-components entirely missing, with app.html's OLD substitute tab still writing the deprecated flat keys. Added to the existing probe's pinned tuple (5 names now, still red, still queued — not a new fix, sharpening F13's existing evidence). Tonight's F34 KG copy fix confirmed verbatim-identical in both files. **[SYNC 2026-09-04: F13 is now FIXED, but not by porting the components — the "silently regenerate and lose them" danger this finding was actually about was found to already be closed by a guard in build_ui.py predating this whole audit by 9 days; the probe was rewritten to verify that guard instead of asserting the mirror's staleness (which is accepted by design). F7 reviewed and left as no-action-needed for the same reason (app.html's flat-key divergence is dead/unserved code the guard already protects). Sweep count unchanged.]** |
| Repo → installer payload (packaging/windows) | SWEEP 3 CLEAN | Round 2: Q5 (embedder "arrives with install" claim false on the installer's own recommended no-GPU path, queued, low severity), model-id defaults vs. installer ladder HOLD, INSTALLATION.md vs install.ps1 sizes/steps HOLD, jobs/skills top-level-import concern confirmed scoped to pip-install only (Windows installer payload includes both dirs, HOLDS), no Breeze TTS2 or other NC-licensed leakage found. Round 3 (this sweep): model-id ladder re-verified to two decimals against model_plan.py's footprint formula; one divergence found (scripts/install.ps1, a separate legacy one-liner, still hardcodes gemma3:4b) traced and confirmed already disclosed accurately in docs/INSTALLATION.md — not new. No second instance of Q8's "computed but never gates" class found across all of packaging/windows (disk/RAM/hash/network checks each individually verified). Packaging manifest is exclude-list-based and can't silently drop src/skills/optional-skills/data; no new third-party import risk from this branch's changed files. Breeze TTS2 and all other assets reconfirmed clean of NC licensing. Q5/Q8 remain the only open items after round 3. **[SYNC 2026-09-04: both Q5 and Q8 now FIXED — see findings.jsonl.]** Round 4 (this sweep, a genuinely different angle — post-install first-run promises vs. setup_wizard.py, upgrade-path re-check against 3 historical KNOWN_ISSUES.md incidents, uninstall.ps1's actual behavior vs. its contract): upgrade path confirmed clean (all 3 historical incidents — the -Verify-before-action bug, the start.bat/vault-passphrase deletion, the wizard's silent-overwrite default — independently confirmed fixed in current code); uninstall.ps1 confirmed to do exactly what it claims (vault data + its credential always deleted or kept together, never split; unlisted future data folders default to preserved). But found F38 — README.md's "greets you by voice" onboarding claim is false; both the browser and CLI wizards are silent. Fixed. Needs a 5th clean sweep (reset by this round's real finding). |
| Voice pipeline end-to-end | SWEEP 6 FOUND NEW (F39: the "second tab silently kills the first tab's call" path never notified the user, unlike every other unprompted-ending path in the same function — FIXED) — needs a 7th clean sweep | Round 1 (via the F4 investigation) confirmed the screenshot/vision-attachment sub-path respects local-only routing (H9, HOLDS, fixed 2026-08-23). Round 2's prompt-assembly finder also confirmed routes/voice.py's Gemini Live text/tool-result gating HOLDS. Round 4: F27 (TTS ignored local_preferred, FIXED). Round 5: F33 (renewal loop never rechecked local_only, FIXED); STT/barge-in/Tier-1-2 fallback all confirmed clean. Round 6 (this sweep, cost-compounding/session-cleanup/multi-session angles): cost compounding confirmed to hold as already captured by Q6 (degree, not a new defect); session cleanup (Gemini handle, audio buffers, orb registry) confirmed clean, one theoretical orb-leak path already self-heals via routes/tasks.py's existing orphan-reaper within 30 min. But found F39: a second browser tab silently kills the first tab's in-progress call with zero notification — fixed. Q6's unmetered Gemini TTS/Live billing gap still open from round 2. **[SYNC 2026-09-04: Q6 part (c), the Gemini TTS/Live metering gap this row names, is now FIXED. Q6 parts (a)/(b) are a separate, still-open/escalated policy question tracked under the "Cost and budget accounting" row below, not part of what this row was about.]** |
| Scheduler & workflows | SWEEP 4 FOUND NEW (F41: builtin-task tracebacks were silently discarded under the packaged app's pythonw runtime — FIXED; Q24: manual Run Now can race a schedule into concurrent double-execution, and the KG nightly job bypasses the manual route's own lock — queued; Q25: a whole phantom Settings→Scheduler UI section polls and renders nothing — queued) — needs a 5th clean sweep | Round 1 found + fixed the orphaned nightly KG reindex job (F1). Round 2: F25 (builtin tasks ship with zero retry tolerance, queued), Q15 (away_drain has no UI on-switch, incomplete feature not a regression). Round 3: all 19 registered builtin/scheduled tasks walked exhaustively (all reachable, schedules match docs); routes/workflows.py and workflow_plan.py covered (F17 reverified correct, work_plan.py's API correctly wired). Found Q18. Round 4 (this sweep, error-handling/concurrency/route-vs-service angle): found and fixed F41; found Q24 (a real concurrency gap, needs a design decision on Run Now's intended semantics before fixing); found Q25 (dead UI, low severity). **[SYNC 2026-09-04: F25, Q18, Q24 all now FIXED; Q25 FIXED (removed, not built — the dead Settings→Scheduler section is gone, the real Workflows-tab scheduler is untouched). Q15 reviewed and left as documented, deliberately-incomplete feature (no action needed). Sweep count unchanged.]** |
| Startup wiring | **UNPARKED AND SWEPT (2026-09-04, Stephen's explicit go-ahead — "consider it unparked... make the code change you need").** Static reachability tracing complete; dynamic boot executed for real. | The original parked verdict (env vars alone can't isolate conditions 3/4) was correct and is superseded, not overturned: Stephen explicitly authorized the code change the static analysis said dynamic proof would require — not stubbing `_mcp_boot`/`bootstrap_provider_env`/the residency arbiter into inertness (which would have defeated the purpose), but building a real, isolated `.friday` home (redirected before any `agent_friday` import, never touching Stephen's real one) and launching the actual `src/agent_friday/server.py` as a genuine subprocess, on a distinct port, with `FRIDAY_TESTING` unset — the one thing that had never happened. Stephen's live Friday (PID 39784, port 3000) confirmed running throughout, untouched; GPU/Ollama confirmed returned to exact baseline afterward. Found 4 new findings, all from watching rather than reading: **F67 (SEVERE)** — 6 real, currently-registered provider credentials (MISTRAL_API_KEY, DEEPSEEK_API_KEY, XAI_API_KEY, FIREWORKS_API_KEY, PERPLEXITY_API_KEY, COHERE_API_KEY) are absent from `extension_security.ENV_BLOCKLIST` and leak into any sandboxed-trust MCP connector's real environment — confirmed by a live spawn capturing a planted fake Mistral key while a blocklisted Anthropic key correctly did not leak, the third recurrence of F32/F44's exact defect class. **F68** — `provider_key_status()` reports "connected" from file-existence alone, never from decryptability; reproduced Stephen's own real situation (3 provider keys currently undecryptable) in isolation and confirmed all 3 report "connected" identically to working keys, with the boot log's bare "loaded N" count giving zero indication anything failed. **F69** — `hardware_profile.py`'s WDDM VRAM-reserve reader is live-broken on this actual machine right now (an impossible 42,301 MiB reading, correctly caught and floored to 1024 MiB) — fresh evidence reinforcing F62. **F70** — residency boots on an unsynchronized daemon thread with no ordering guarantee relative to the HTTP port opening; confirmed safe in this run only because no LLM needed loading. Also confirmed: MCP connectors spawn (and their environment is fixed) at least ~28 seconds before the HTTP port is reachable at all — no boot-time consent gate exists or could exist, structurally. All 4 findings `confirmed_pending_action` (mechanical fixes vs. deeper design questions flagged per finding, not decided unilaterally) — see findings.jsonl F67-F70 and progress.md's Round 14 for full method and evidence. **[SYNC 2026-09-04: F67 and F68 both now FIXED per Stephen's direct rulings on each — F67 inverted from a denylist to an explicit allowlist (`SANDBOXED_ENV_ALLOWLIST`) rather than patching a 4th missing name into `ENV_BLOCKLIST`; F68's `provider_key_status()` now attempts a real decrypt and reports 3 states (connected/present_but_unreadable/missing) instead of inferring from file existence. Both red-on-revert proven — see findings.jsonl and progress.md's Round 17. F69/F70 remain open design/reliability questions, not re-escalated here.]** |
| Knowledge graph ingest → retrieval | SWEEP 5 FOUND NEW (F37: the always-on "related pages" context injection bypassed tier gating entirely, a TIER_3 encrypted-section page's real content could reach any provider including cloud — FIXED) — needs a 6th clean sweep | Round 1: F1 (nightly reindex orphaned, FIXED), H4 (indexer method-name fix confirmed correct and now reachable via the fix). Round 2's prompt-assembly finder additionally confirmed KG retrieval content is double-protected (sensitivity-based model pinning + full egress classification) before reaching cloud. Round 3 (live-incident investigation, see progress.md top section and F31): traced a real overnight cost leak to reindex_tier_b's uncapped extraction loop; added MAX_CLOUD_EXTRACT_CALLS, proven red-green-red. Round 4: F34 — _resolve_model()'s `pinned` flag was computed and discarded; fixed. Round 5 (this sweep): all 3 of reindex_tier_b's call(...) sites confirmed to inherit F34's pin-enforcement correctly (they share the same _llm() chokepoint); retrieval.py itself confirmed clean, reusing the same chokepoint. But found F37 in a sibling always-on context path (knowledge_context_block → context_injection → every system prompt) — fixed. Q9/Q10 re-verified still accurate. Found Q23 (F31's own cap interacts badly with the manifest's per-file granularity — some multi-chunk-file chunks can be silently, permanently skipped in gated_cloud mode; queued, not the default mode). **[SYNC 2026-09-04: Q9 FIXED; Q23 FIXED. Q10 remains genuinely open — re-examined at Stephen's request and kept as a specific escalated question (correction semantics + unbounded growth; see progress.md's Round 9 for why an eviction policy wasn't invented unilaterally) rather than force-closed. Sweep count unchanged.]** |
| MCP and connector registration | **CLOSED — SWEEP 2 CLEAN (2 consecutive)** | Round 2: F8 (restart() bypassed the security-disable gate, FIXED), F9 (status mislabeling for a security-blocked connector, queued — the observability half of the same cluster), H10 (registration→tool-callable chain, OAuth expiry disclosure, local-seat tool dropping disclosure, no orphaned registration function — all confirmed HOLDS). Round 3: found and fixed F32 (every stdio MCP server inherited Friday's full decrypted-secrets environment). Round 4: found and fixed F36 (tool-call sanitize/audit mechanism never wired in on either transport) — Q22 (minor, allowlist keyed by name) queued alongside. Round 5 (full end-to-end re-read of mcp_client.py + extension_security.py + all MCP routes/wiring): F8/F28/F32/F36 all confirmed holding; no trust-level-chokepoint bypass anywhere; F9/Q22/H10 unchanged. Clean (1st). Round 6 (this sweep, a genuinely different angle — frontend trust-level UI, API.md/FILE_GRANTS.md doc claims re-checked against F36, MCPServerProcess concurrency traced under two threads, every MCPServerHTTP construction/attribute site checked for the new trust_level param): all clean, no false UI claims, no doc drift, no race, no crash site. Clean (2nd) — **seam formally closed** per the audit's own 2-consecutive-clean exit bar. F9 and Q22 remain open/queued (a closed seam still carries its queued items forward to Stephen; closure means the SWEEP found nothing new twice in a row, not that every queued item is resolved). **[SYNC 2026-09-04: F9 and Q22, the last two open items this closed seam was still carrying, are now both FIXED — see findings.jsonl. The seam's closure status (2 consecutive clean sweeps) is unaffected either way; this just means it's no longer carrying anything queued at all.]** |
| SkillOpt (skill capture, scoring, promotion) — seam 13, new 2026-09-04 | SWEEP 1 FOUND NEW (F72, F73, F74 fixed; F75 escalated) — needs a 2nd clean sweep | Opened from an external review Stephen commissioned (read the public v5.10.0 repo, predating this whole audit) rather than this audit's own sweep methodology — its 4 claims were re-verified against current code before being trusted, not assumed. F72: cognitive_memory.memory_rollback() was named and documented as point-in-time restore but is structurally incapable of it (write_memory() keeps no per-key version history) — reproduced by direct execution (write A, cutoff, write B, rollback → neither A nor B survives), fixed as a documentation correction (real restore would need a genuine architecture change, left as an open feature question, not built unilaterally). F73: SkillOptEngine.run_epoch() could promote a candidate with zero real evaluation behind it — an evaluator that raised on every case, no evaluator configured with no execution history, and an empty eval_batch all collapsed candidate/baseline scores to an identical fallback value that the gate's own tie-tolerance rule always passes; reproduced all 4 scenarios the review asked for by execution, fixed by refusing to promote when zero cases were genuinely evaluated (completing the dataclass's own already-declared but previously-unproduced "inconclusive" decision state), red-on-revert proven. Currently unreachable from any live path (no route/CLI/scheduler calls run_epoch at all) — fixed before the documented-but-missing autoresearch wiring lands, since the one production config (no evaluator) is the exact worst case this bug mishandled. F74: skill_capture.capture() sent {"quality": score, "success": score} into record_skill_run(), but composite_score() never reads those keys — reproduced the reviewer's own figures exactly (both 1.0/1.0 and 0.0/0.0 scored an identical 0.25); fixed narrowly by routing the signal into `accuracy` only (the one dimension the heuristic can honestly speak to), matching the correct convention liquid_ui.py's sibling caller already uses, red-on-revert proven. F75 (companion to F74, explicitly coupled by Stephen's own framing): the underlying success detector (_success_score()) is a reply-shape heuristic only — reproduced the reviewer's own example exactly ("Done. I sent the email and booked your flight." scores 1.0 with zero task verification) — NOT fixed; a mechanical patch (downgrade when prose claims an action but tool_trace is empty) was considered and rejected as its own new defect (would incorrectly downgrade skills that legitimately need zero tools), so this is escalated to Stephen as a genuine product decision about what "success" should mean per skill, with two directions offered, neither chosen. |
| Cost and budget accounting | SWEEP 3 FOUND NEW (Q11: ElevenLabs TTS + 5 opt-in provider catalogs meter as $0, queued; Google API metering gap closed with a doc comment) — needs a 4th clean sweep | Round 2: Q6, three-part finding, all queued — (a) USD budget alert-only-by-design and off by default (documented intentional, root cause of the historical incident, still unaddressed), (b) budget_enforcer fail-closed but scoped to a synthetic unit disconnected from USD and explicitly fail-open-on-error, (c) Gemini TTS/Live voice calls completely unmetered despite PRICING table entries showing clear intent to track them — highest-priority sub-item. routes/costs.py confirmed to read the live ledger with no drift (HOLDS). Checked the finder's minor note that GET /api/costs/budget lacks @login_required while the sibling POST has it: all three OTHER GET routes in the same file (/api/costs/summary, /timeseries, /scheduled) are equally unguarded, so this is the file's consistent read-vs-write pattern, not an anomaly — HOLDS, not a fix target. Round 3 (this sweep, dispatched after the live F31 incident): generalized F31's exact defect shape (unbounded cloud-eligible LLM calls in an unattended loop, no per-run cap) across every other `_generate_text`/`_generate_agent` call site and every scheduler builtin task — none match; each is either a fixed-small number of calls or already has an explicit cap (goals.py's `max_per_tick=5`, session_summary's 3-day loop, content_pipeline's `claim_due_targets(limit=50)`, etc.). `qa_gates.gate_text()` confirmed to have zero callers anywhere (dead code, not a live risk). One adjacent-but-distinct item surfaced and queued: Q16 (creations.py's autonomous daily short-production mode bypasses all 3 of creative_pipeline's human-review checkpoints via `until_checkpoint=False` — bounded, not F31's shape, but a real unattended-spend/no-review gap). Round 4 (this sweep): walked the full cost_meter.PRICING table against every scheduler builtin's call sites — F31's exact mechanism does not recur anywhere else; the DB's single-writer + dual-lock write path confirmed safe under concurrent background-task completions; Q11 and Q7 (image/video/music generation completely unmetered, poisoning the daily-creation budget gate's own spend calculation) re-verified still accurate, not new. Found Q21 (no job-duration anomaly signal anywhere in the API surface — a builtin scheduled task has no timeout at all, unlike agent_prompt-kind schedules' explicit 1800s cap — the direct answer to "how would we notice a runaway job like F31 sooner next time"). Round 5 (dedicated root-cause investigation, per Stephen's explicit request to find the systemic reason rather than keep finding individual instances): confirmed and sharpened Q13's judgment — text-path metering is a side effect of the chat routing/fallback ladder that had to exist anyway, not a metering feature in its own right, and image/video/music/voice generation each have exactly one provider so no router (and thus no metering chokepoint) was ever built for them; a true unified fix (mirroring egress's gate_text single-primitive solution) is NOT realistic because cost, unlike sensitivity, needs the RESPONSE in a different shape per modality (tokens/characters/seconds/images), not just the request text. Broke the remaining gap into what's genuinely mechanical (image-gen usage_metadata already present but unread; ElevenLabs character-count already in scope; the 5 extra provider catalogs are already architecturally centralized, $0 is purely missing pricing DATA) versus what needs real per-provider design work (Veo/Lyria duration-based billing, Gemini Live per-session-second billing) versus the durable structural fix (a required-usage-record interface every future non-chat integration must satisfy). Filed as Q26 — supersedes nothing, deepens Q13/Q6/Q7/Q11 with an actionable priority order. Not implemented: even the "mechanical" items need a verified real-world rate before touching a real-money ledger, which this run correctly declined to fabricate unattended. **[SYNC 2026-09-04, and a real correction, not just a status update: Q7 and Q11 ARE now fixed, but the first attempt at finishing them violated the exact "don't fabricate a rate" discipline this row's own round-5 text praises — two rates shipped as unverified interpolations, both wrong by 2x, caught and logged as a self-finding (F50) alongside a broader pricing-fabrication cleanup. Q13/Q26 themselves are now marked no-action-needed/superseded — their concrete predictions are the fixes above; one forward-looking architecture recommendation (a required-usage-record interface) is noted, not built, since nothing currently unmetered depends on it. Q6(c) FIXED; Q6(a)/(b) re-examined at Stephen's request — (b) turned out to be a misdiagnosis (an unrelated internal virtual-economy system, not a real-dollar gap); (a)'s UI already honestly discloses "alerts," so it's a pure policy fork, kept escalated. Q16 (named in this row's round-3 text as "adjacent but distinct") is now FIXED — Stephen decided it directly. Q21 FIXED. See progress.md's DELEGATION RESOLUTION and Round 9 sections for full reasoning. Sweep count unchanged — no fresh sweep run today.]** |

## Claim corpus extraction

**[SYNC 2026-09-04, evening: the docstring walk named directly below is now
COMPLETE. 5 parallel fresh-context agents covered all ~140 remaining
`src/agent_friday/services/*.py` modules (28 each, alphabetical chunks);
combined with the 12 already walked, every service module's docstrings have
now been read and checked at least once. Result: 94 new HOLDS claims
(C150-C243, closing the "flat at 149" gap) and 10 real findings, all 10
triaged to a fixed disposition (F53 through F64 — see findings.jsonl and
progress.md's Round 11 for the full list). claims.jsonl is current as of
this sync. What's still NOT done: `index.html`/`ui_parts/app.html`'s full
line-by-line disclosure-string corpus beyond what F45/F46/Q19/F11 already
covered, and a second independent pass over the now-243-claim corpus (this
was each module's FIRST walk, not a second confirming sweep) — both remain
open for a future round.]**

**Correction 2026-09-04 (Stephen's check-in):** this table previously said
UNSTARTED for every source despite progress.md describing a completed round-1
claim-extraction pass and 6 agents having returned full claim lists — that
was a bookkeeping failure (the claims were reported back in agent transcripts
but never transcribed into claims.jsonl or reflected here). claims.jsonl now
holds C1-C90, extracted from the round-1 and round-2 agent reports, each
with a quote, location, and either a `linked_finding` (F#/H#) or a `status`
of `verified_holds` / `verified_BROKEN*` / `noted_not_independently_chased`
(extracted and read, but not individually traced to a verdict this round —
honestly marked as such, not silently dropped).

**Second correction 2026-09-04 (Stephen's morning check-in):** the corpus
had genuinely stopped growing after that — flat at C1-C90 from 00:51
until now, meaning nothing extracted since round 2 had been judged at
all. Stephen named `routes/*.py` docstrings specifically as unwalked;
that gap is now closed (C91-C133, all 61 route files read at the
docstring level — see the updated row below). No other corpus source has
been extended since round 2; the other "PARTIAL"/"NOT SYSTEMATICALLY
DONE" rows below are still exactly that.

| Source | Status |
|---|---|
| THREAT_MODEL.md | FULLY EXTRACTED AND CHASED (C1-C13, C134-C135) — all 298 lines read; every one of the 9 originally noted-not-chased claims traced into current code (8 hold, 1 broken: F42); 2 additional claims extracted and checked (1 broken: F43, fixed; 1 broken: Q27, queued) |
| KNOWN_ISSUES.md (incl. 2026-09-03 entries) | EXTRACTED (C14-C41) — most "Fixed" entries independently re-verified against current code, several still-open items confirmed still open |
| docs/FILE_GRANTS.md | EXTRACTED (C42-C49) — 5 rules noted, 1 partially verified (no-grant-tool half); deeper per-rule verification not yet done |
| README.md | EXTRACTED (C50-C59) |
| RELEASE_NOTES.md | EXTRACTED (C60-C63) — historical entries, low priority for this release's focus |
| docs/CONFIGURATION.md | EXTRACTED (C64-C67) — defaults cross-checked against DEFAULT_SETTINGS |
| docs/API.md | EXTRACTED (C68-C73) — route-registration cross-check done (no documented-but-missing routes found) |
| tests/README.md | EXTRACTED (C74-C77) — 2 of 4 claims checked (route count, suite timing) found to be planning claims rather than assertions/facts, per Stephen's explicit correction; treated as corpus items to judge, not trusted |
| index.html (settings labels, disclosures, Saved affordances) | EXTRACTED (C78-C82) — settings-panel toggles covered; broader disclosure-string sweep (onboarding aside) not exhaustive |
| ui_parts/app.html (same) | EXTRACTED (C136-C143 among C136-C149) — full disclosure-string surface read end to end (tooltips, settings copy, onboarding/wizard text, kill-switch/toast copy), not just the component-presence divergence check; found F45, F46, and 3 more loci of Q19/F11's already-known defects |
| src/agent_friday/services/*.py docstrings | **COMPLETE — all ~153 modules walked at least once.** First 12 (onboarding_copy.py, provider_health.py, cost_meter.py, egress_gate.py, creative_engine.py, music_engine.py, sensitivity_classifier.py, vault_passphrase.py, connector_secrets.py, extension_security.py, wiki_engine.py, federation.py — C83-C90, C136-C149) found F44 (severe, fixed) and C144. The remaining ~140 (5 parallel agents, alphabetical batches of 28) found 94 more HOLDS (C150-C243) and 10 real findings, all fixed this round: F53 (memory_proposals.py, no caller anywhere — added routes/memory_proposals.py), F54 (content_policies.py, SEVERE — H1-H4 harm floor silently bypassed for category-only content), F55 (boot_guard.py/server.py — boot confirmed by a timer, not a real served request), F56 (scoped_agents.py — cleanup no-op + tool-permission enforcement overclaim), F57 (research/harness.py — integrity check always TypeError'd, silently swallowed, never actually ran), F58 (predictive_workspaces.py — prewarm resolver looked up names nothing ever imported, always a no-op), F59 (prompt_manager.py — dead, self-contradictory constructor, removed), F60 (ambient_awareness.py, docstring only — 2 of 3 claimed adaptive behaviors computed but never consumed), F61 (compute_client.py, docstring only — Orchestrator never actually calls this), F62 (gpu_headroom.py, docstring only — the one subsystem its origin story is about uses a different implementation), F63 (6 bundled minor doc-drift corrections). F64: a pre-existing tests/unit+tests/api order-dependence defect (15 tests fail full-suite-only) found while verifying this round didn't regress anything — confirmed unrelated to this round's changes via a stash-based A/B against clean HEAD, logged not fixed (root-causing which earlier test pollutes state is a separate investigation). See progress.md's Round 11 for full detail on each. |
| src/agent_friday/routes/*.py docstrings | EXTRACTED (C91-C133) — all 61 route files read at the module-docstring level (or confirmed to carry only a shared boilerplate header); ~30 files had at least one checkable factual claim worth extracting, ~30 were pure route-enumeration with nothing to extract. Verified on the spot (not just extracted): all 15 claims that looked strong enough to double-check held against the actual code, no contradiction found. Depth still shallow on the ~15 largest files (chat.py, code.py, insights.py, news.py, workflows.py) — function-level docstrings there were sampled via grep, not read end to end. |
| Implicit claims: exported fns / UI controls / settings keys / routes / scheduled jobs / worker adapters actually reachable | IN PROGRESS — this is the core method used throughout (F1, F8, F9, F12 are all reachability findings), not a one-time pass; continues every round |
