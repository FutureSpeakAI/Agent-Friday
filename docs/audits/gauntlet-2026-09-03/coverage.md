# Seam coverage

Legend: UNSTARTED / IN PROGRESS / SWEEP 1 CLEAN / SWEEP 2 CLEAN (done) / PARKED

## Exit criteria (corrected 2026-09-04, per handoff from the session that
## wrote the gauntlet-loop prompt — see progress.md "Handoff" section)

The prompt's original wording ("two consecutive fresh-context sweeps ...
and every claim on it holds blind") cannot be satisfied for a seam carrying
a BROKEN finding whose fix is queued rather than landed — a queued fix
never "holds." The corrected exit condition, which this run uses:

A seam is done when **two consecutive fresh-context sweeps surface nothing
new**, AND **every claim on it carries a verdict with an artifact**:
- HOLDS — the observation/call-graph that established it (findings.jsonl `H*` entries), or
- BROKEN/UNREACHED — a red probe under tests/gauntlet/ pinning it (fixed: probe now green with red/green/red evidence; queued: probe stays red, evidence + reason in progress.md's QUEUED FOR STEPHEN section).

Green-everywhere is not the bar. Judged-everywhere is.

| Seam | Status | Notes |
|---|---|---|
| Settings write → settings read | SWEEP 5 FOUND NEW (Q17: off_record's "for this session" copy claim is false — nothing ever resets it, queued) — needs a 6th clean sweep | Round 1: F3 (context_retention_days, queued+probe-pinned), F6 (camera_auto_describe, dead, low-priority), 7 HOLDS confirmed. Round 3: F24 (persona/distribution presets mostly placebo, queued). Round 4 (this sweep): confirmed General/Appearance dead toggles (scene_name, compact_mode, startup_workspace, auto_open_chat, stream_responses) are already tracked in KNOWN_ISSUES.md and the settings-readers-check allowlist — not re-reported. Connectors tab confirmed read-only display (no new write-path risk). No dedicated Notifications settings panel exists to sweep. Found Q17. |
| Prompt assembly → provider payload | SWEEP 1 CLEAN (F12 verified unreachable today, fixed as hardening anyway) | Round 2: KG retrieval, context_injection, web_fetch/firecrawl, /api/analyze, and routes/voice.py's Gemini Live gating all traced and HOLD. F12: egress_gate.py has zero handling of the OpenAI-compatible `tool_calls` wire field (vs. the already-fixed Anthropic `tool_use` equivalent) — structural gap confirmed, but the two obvious cross-call vectors are incidentally closed. A dedicated verifier is checking the one open question (worker/orchestrator/sub-agent retry paths carrying tool_calls cross-provider) before this seam can close. |
| Router request → seat actually served | SWEEP 6 FOUND NEW (Q19: local_only/local_preferred's own "ALL turns"/"local first" promise is false for ordinary interactive tool-use chat — SEVERE, queued; Q20: task_overrides.voice is permanently inert, TaskType.VOICE unreachable, queued) — needs a 7th clean sweep | Round 1: F2 (provider_health gaps, FIXED), H2/H3/H6 confirmed HOLDS. Round 5: F30 (fallback ladder reuses gated-for-wrong-provider prompt, queued, systemic), Q12 (last_local_refusal race, latent/inert, observed). Round 6 (this sweep): _apply_cloud_provider substitution risk checked and clear (registry-first resolution + colon-first provider_family check catch every local-model shape reachable in practice); no second, inconsistent seat-resolution function found (model_seat_gate.resolve_local_seat is a documented no-op, not a competitor); no stale conversation_seat override survives a mid-conversation settings change (verified fresh-loaded every turn, plus _sync_capability_routing keeps the watched orchestrator_model key mirrored). But found Q19 (severe — see progress.md's top section) while checking _route_basic's other branches. |
| UI copy ↔ code (index.html / app.html labels vs behavior) | SWEEP 2 FOUND NEW (KG "local only = nothing ever leaves this machine" claim, FIXED via F34) — needs a 3rd clean sweep | Round 1 covered settings-panel toggles (F3/F5/F6/F7). Round 2 covered onboarding_copy.py (F10 — SEVERE, local_only's "nothing sent, ever" is false by default, queued as top priority; F11 — vault-passphrase copy overstatement, queued; H11 — 6 other claims confirmed accurate). Round 2b (this sweep, triggered by F31/F32 context): index.html/app.html's KG-settings copy ("Local only = nothing ever leaves this machine") was false the moment a cloud model was bound as the reasoning seat — same root cause as F34, fixed there (indexer._llm() now enforces the pin directly), so the copy is true again with no separate copy edit needed. MCP/Connectors panel copy and the settings-save toast path checked clean. Needs 2 more consecutive clean sweeps given how much this seam keeps finding. |
| index.html ↔ ui_parts/app.html divergence | SWEEP 1 CLEAN (F13: stale "17" count corrected, real gap is 14 and growing, 4 high-severity components pinned+queued) | Round 1 found the orchestrator_model/creative_model flat-key divergence (F7, HOLDS-with-latent-risk, not fixed — currently works). Full component-count diff (KNOWN_ISSUES.md's "17 components the mirror lacks" claim) not independently re-verified yet. |
| Repo → installer payload (packaging/windows) | SWEEP 3 CLEAN | Round 2: Q5 (embedder "arrives with install" claim false on the installer's own recommended no-GPU path, queued, low severity), model-id defaults vs. installer ladder HOLD, INSTALLATION.md vs install.ps1 sizes/steps HOLD, jobs/skills top-level-import concern confirmed scoped to pip-install only (Windows installer payload includes both dirs, HOLDS), no Breeze TTS2 or other NC-licensed leakage found. Round 3 (this sweep): model-id ladder re-verified to two decimals against model_plan.py's footprint formula; one divergence found (scripts/install.ps1, a separate legacy one-liner, still hardcodes gemma3:4b) traced and confirmed already disclosed accurately in docs/INSTALLATION.md — not new. No second instance of Q8's "computed but never gates" class found across all of packaging/windows (disk/RAM/hash/network checks each individually verified). Packaging manifest is exclude-list-based and can't silently drop src/skills/optional-skills/data; no new third-party import risk from this branch's changed files. Breeze TTS2 and all other assets reconfirmed clean of NC licensing. Q5/Q8 remain the only open items. Needs 2 more consecutive clean sweeps to formally close per exit criteria (this is the 1st of 2). |
| Voice pipeline end-to-end | SWEEP 5 FOUND NEW (F33: /ws/live's local-only gate never rechecked across an hours-long call's renewal legs, FIXED) — needs a 6th clean sweep | Round 1 (via the F4 investigation) confirmed the screenshot/vision-attachment sub-path respects local-only routing (H9, HOLDS, fixed 2026-08-23). Round 2's prompt-assembly finder also confirmed routes/voice.py's Gemini Live text/tool-result gating HOLDS. Round 4: F27 (TTS ignored local_preferred, FIXED). Round 5 (this sweep): STT path confirmed to inherit the same connect-time gate as TTS (no separate STT bypass); barge-in reviewed fresh and holds (one documented, accepted worst-case delay, not a contradicted claim); Tier-2/Tier-1 fallback confirmed structurally local-only-safe (no code path in local_voice.py/nemo_voice.py reaches a cloud provider). But found F33: the renewal loop that keeps a Gemini Live call alive across many ~10-min legs never rechecked local_only before redialing — fixed. Q6's unmetered Gemini TTS/Live billing gap still open from round 2. |
| Scheduler & workflows | SWEEP 3 FOUND NEW (Q18: looks_heavy() has zero callers, the module's stated "ask before heavy work" premise is unreachable, queued) — needs a 4th clean sweep | Round 1 found + fixed the orphaned nightly KG reindex job (F1). Round 2: F25 (builtin tasks ship with zero retry tolerance, queued), Q15 (away_drain has no UI on-switch, incomplete feature not a regression). Round 3 (this sweep): all 19 registered builtin/scheduled tasks (16 in scheduler.py + 3 from publisher.py/analytics_collector.py, correctly pre-seeded before start_scheduler() per an explicit comment guarding against F1's exact failure mode) walked exhaustively — all reachable, schedules match their docs. routes/workflows.py and workflow_plan.py (the "workflows" half, never swept before) covered: F17's fix reverified correct under fresh eyes; work_plan.py's proposal/queue API correctly wired end to end. Found Q18. |
| Startup wiring | PARKED (dynamic, confirmed unprovable by env vars alone) / IN PROGRESS (static) | Read server.py:220-401. Finding: `if not _TESTING:` at module scope (line 317) gates the ENTIRE daemon cascade — MCP connector launch (`_mcp_boot()`, line 375), real provider-key decryption into env (`credential_store.bootstrap_provider_env()`, line 321-326), residency/GPU boot (line 400), scheduler, network monitor, connector-health monitor — as a side effect of merely *importing* server.py, not of reaching `__main__`. `FRIDAY_NO_ARBITER=1` only gates the residency/GPU thread (line 399); it does NOT gate MCP boot or credential decryption. So env vars alone cannot satisfy conditions 3 (no MCP servers) or 4 (no real provider key) — and per existing memory ([[gotcha_isolated_home_does_not_isolate_keychain]]) redirecting HOME/USERPROFILE does not isolate the Windows keychain or the GPU either, so condition 1/4 aren't safe that way either. Verdict: cannot prove all four conditions with a launcher built from env vars + redirected profile. Dynamic boot stays parked; would need to actually patch/stub `_mcp_boot`, `bootstrap_provider_env`, and the residency arbiter before import, which is a code change, not a launch config — out of scope for an unattended overnight run. Independently reconfirmed by the aborted Fable 5.1 session's parallel static read (see progress.md Handoff item 3) — same evidence, same call. Static reachability tracing continues. |
| Knowledge graph ingest → retrieval | SWEEP 4 FOUND NEW (F34: local_only's own "pinned" model was never actually enforced downstream — a cloud-bound reasoning seat could still receive every chunk including TIER_3, FIXED) — needs a 5th clean sweep | Round 1: F1 (nightly reindex orphaned, FIXED), H4 (indexer method-name fix confirmed correct and now reachable via the fix). Round 2's prompt-assembly finder additionally confirmed KG retrieval content is double-protected (sensitivity-based model pinning + full egress classification) before reaching cloud. Round 3 (live-incident investigation, see progress.md top section and F31): traced a real overnight cost leak to reindex_tier_b's uncapped extraction loop; added MAX_CLOUD_EXTRACT_CALLS, proven red-green-red. Round 4 (this sweep, triggered by F31/F32 context): found F34 — _resolve_model()'s `pinned` flag was computed and discarded; _llm() always went through the general router regardless. Fixed: pinned calls now go straight to _call_ollama. Q9/Q10 (forget-me description leak; no correction-invalidation + unbounded growth) still queued from round 2. |
| MCP and connector registration | SWEEP 4 FOUND NEW (F32: every stdio MCP server inherited Friday's full decrypted-secrets environment, FIXED) — needs a 5th clean sweep | Round 2: F8 (restart() bypassed the security-disable gate, FIXED), F9 (status mislabeling for a security-blocked connector, queued — the observability half of the same cluster), H10 (registration→tool-callable chain, OAuth expiry disclosure, local-seat tool dropping disclosure, no orphaned registration function — all confirmed HOLDS). Round 3 (this sweep): start_all/authorize/restart/call all now correctly gate on 'disabled' (F8's fix holds under fresh eyes), no register-before-gate race, FILE_GRANTS.md scoping claims don't apply to MCP process env — but found F32: extension_security.sanitize_env_for_mcp()/ENV_BLOCKLIST had zero callers anywhere, so _spawn() handed every subprocess the ENTIRE parent env including live decrypted cloud-provider keys. Fixed with a trust-level-aware filter, proven red-green-red. Needs a 5th clean sweep (reset by this round's real finding — the 2-consecutive-clean bar starts over from here). |
| Cost and budget accounting | SWEEP 3 FOUND NEW (Q11: ElevenLabs TTS + 5 opt-in provider catalogs meter as $0, queued; Google API metering gap closed with a doc comment) — needs a 4th clean sweep | Round 2: Q6, three-part finding, all queued — (a) USD budget alert-only-by-design and off by default (documented intentional, root cause of the historical incident, still unaddressed), (b) budget_enforcer fail-closed but scoped to a synthetic unit disconnected from USD and explicitly fail-open-on-error, (c) Gemini TTS/Live voice calls completely unmetered despite PRICING table entries showing clear intent to track them — highest-priority sub-item. routes/costs.py confirmed to read the live ledger with no drift (HOLDS). Checked the finder's minor note that GET /api/costs/budget lacks @login_required while the sibling POST has it: all three OTHER GET routes in the same file (/api/costs/summary, /timeseries, /scheduled) are equally unguarded, so this is the file's consistent read-vs-write pattern, not an anomaly — HOLDS, not a fix target. Round 3 (this sweep, dispatched after the live F31 incident): generalized F31's exact defect shape (unbounded cloud-eligible LLM calls in an unattended loop, no per-run cap) across every other `_generate_text`/`_generate_agent` call site and every scheduler builtin task — none match; each is either a fixed-small number of calls or already has an explicit cap (goals.py's `max_per_tick=5`, session_summary's 3-day loop, content_pipeline's `claim_due_targets(limit=50)`, etc.). `qa_gates.gate_text()` confirmed to have zero callers anywhere (dead code, not a live risk). One adjacent-but-distinct item surfaced and queued: Q16 (creations.py's autonomous daily short-production mode bypasses all 3 of creative_pipeline's human-review checkpoints via `until_checkpoint=False` — bounded, not F31's shape, but a real unattended-spend/no-review gap). Needs 1 more clean fresh-context sweep.|

## Claim corpus extraction

**Correction 2026-09-04 (Stephen's check-in):** this table previously said
UNSTARTED for every source despite progress.md describing a completed round-1
claim-extraction pass and 6 agents having returned full claim lists — that
was a bookkeeping failure (the claims were reported back in agent transcripts
but never transcribed into claims.jsonl or reflected here). claims.jsonl now
holds C1-C90, extracted from the round-1 and round-2 agent reports, each
with a quote, location, and either a `linked_finding` (F#/H#) or a `status`
of `verified_holds` / `verified_BROKEN*` / `noted_not_independently_chased`
(extracted and read, but not individually traced to a verdict this round —
honestly marked as such, not silently dropped).

| Source | Status |
|---|---|
| THREAT_MODEL.md | EXTRACTED (C1-C13) — 4 independently verified, 9 noted-not-chased |
| KNOWN_ISSUES.md (incl. 2026-09-03 entries) | EXTRACTED (C14-C41) — most "Fixed" entries independently re-verified against current code, several still-open items confirmed still open |
| docs/FILE_GRANTS.md | EXTRACTED (C42-C49) — 5 rules noted, 1 partially verified (no-grant-tool half); deeper per-rule verification not yet done |
| README.md | EXTRACTED (C50-C59) |
| RELEASE_NOTES.md | EXTRACTED (C60-C63) — historical entries, low priority for this release's focus |
| docs/CONFIGURATION.md | EXTRACTED (C64-C67) — defaults cross-checked against DEFAULT_SETTINGS |
| docs/API.md | EXTRACTED (C68-C73) — route-registration cross-check done (no documented-but-missing routes found) |
| tests/README.md | EXTRACTED (C74-C77) — 2 of 4 claims checked (route count, suite timing) found to be planning claims rather than assertions/facts, per Stephen's explicit correction; treated as corpus items to judge, not trusted |
| index.html (settings labels, disclosures, Saved affordances) | EXTRACTED (C78-C82) — settings-panel toggles covered; broader disclosure-string sweep (onboarding aside) not exhaustive |
| ui_parts/app.html (same) | PARTIAL — covered via the divergence check (F7), not independently re-read end to end |
| src/agent_friday/services/*.py docstrings | PARTIAL — onboarding_copy.py, provider_health.py, cost_meter.py, egress_gate.py docstrings specifically checked (C83-C90); not every service module's docstrings individually corpus-walked |
| src/agent_friday/routes/*.py docstrings | NOT SYSTEMATICALLY DONE — individual routes checked as part of seam work, no dedicated docstring sweep across all ~61 route files |
| Implicit claims: exported fns / UI controls / settings keys / routes / scheduled jobs / worker adapters actually reachable | IN PROGRESS — this is the core method used throughout (F1, F8, F9, F12 are all reachability findings), not a one-time pass; continues every round |
