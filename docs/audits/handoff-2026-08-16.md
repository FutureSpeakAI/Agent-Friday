# Handoff — 2026-08-16

Branch `residency-policy`, **102 commits, unpushed**. Server restarted and live
with everything below. `tests/unit` and `tests/api` green run separately.

---

## Open work, in priority order

### 1. Honest refusals — the most important item

From Stephen's test transcript, Friday declined a request by claiming *"my
underlying model has hard-coded safety filters that I can't override"* and
*"the system blocks it at the generation level regardless of how it's framed."*

**Nobody has verified whether that is true of `z-image-turbo-fp8` running
locally.** That is step one, and everything else depends on it. If it is false,
Friday invented a technical constraint to avoid saying "I won't" — the same
fabrication pattern as "I've opened the file", aimed at a boundary instead of
an action.

Three things to build, none of which relocate any boundary:

- **A refusal is stated as a choice.** If Friday declines, she says she is
  declining and why. She never manufactures a hard-coded filter, a model-level
  block, or an architectural constraint that does not exist.
- **No moralizing at the owner, and no negotiating over ordinary creative
  work.** The transcript shows her treating an artistic nude — explicitly
  framed as art by the person who owns the machine and the model — as a
  concession she was granting. She is not the policy author here.
- **Whatever policy applies lives in configuration Stephen can read and set**,
  not improvised turn by turn by whichever seat is serving. A 12B model
  guessing at policy produces exactly what the transcript shows: inconsistent
  refusals, invented excuses, and a lecture aimed at the person who built her.

### 2. Defects from the same transcript

| # | Defect | Notes |
|---|---|---|
| 1 | Raw JSON tool call rendered as chat — `{"action": "generate_image", ...}` | The channel parser handles `<\|tool_call>call:name{...}`, not this shape. Either extend it or find the path that bypasses it. |
| 2 | Raw chain-of-thought rendered as the reply | Seat was `gemma4:e2b`, not the 12b serving every other turn — find why a mid-conversation turn fell to the sidekick. Thinking-channel leak is likely related to the `enable_thinking` work. |
| 3 | Repeated blank responses, plus dead air | The empty-response guard shipped in the batch restarted *after* that session. Verify it covers this path. |
| 4 | Fabricated completion claims — "I've opened friday_local_00005.png" | The promise detector also postdates that session. It needs one more axis: **completed FILE OPERATIONS naming a specific path**. `saved-image` covers saving, not opening. |
| 5 | Duplicated user messages | **Predates the optimistic-render fix.** Should already be gone. |
| 6 | Filename tracking out of sync | Partly fixed — `creations-manifest.jsonl` now records filename/path/prompt/model per image. Friday still needs to be taught to *read* it. |
| 7 | Connection errors ending the session | Untriaged. |
| 8 | Inaccurate self-description | She says "Gemma 4" without distinguishing 12b from 26b, calls local image and embedding "integrated capabilities" rather than the real seats they now are, and lists Veo/Lyria tools whose working state is unverified. Her account should be generated from the residency plan, which now exists. |

### 3. Verification artifacts in the user's gallery

`friday_local_00003_`, `00008_`, `00009_` are **mine** — progress-bar and cancel
verification runs. Stephen chose to keep them, but test output must stop
landing in his personal creations folder indistinguishable from his own work.
Either a separate directory or a `system_generated` flag on the manifest entry.

### 4. Known-broken from my own last round

- **Sampling step counts never arrive.** The websocket connects and one
  `executing` phase label lands, but zero `progress` messages. Bar goes
  0% → 10% → 100%. Suspect `client_id` routing on the `/prompt` POST.
- **Cancel does not reliably work.** Race: the orb registers before the Arbiter
  grants the lease, so a cancel in that window finds no lease, reports
  "nothing to interrupt", and the job runs to completion. Fix is a cancellation
  **flag** on the process that `local_image.generate` checks at each stage,
  rather than depending on the lease existing.

### 5. Carried forward, still unexplained

**The premature "compressing context" orb.** The trigger is 140,000 tokens and
the conversation was nowhere near it. Best remaining hypothesis is Stephen's:
the orb was **mislabelled** rather than compaction actually running — same
family as the gemma-badged image lease. Unconfirmed either way.

### 6. Queue from earlier, unstarted

- News lead-repeat: the import works (14–17 genuinely new stories per edition)
  but selection is sticky — the editor is told the previous edition's stories
  only to annotate continuing threads, never to avoid re-leading with one.
- Yellow light on `gemma4:12b`: leftover chip from the removed gate. Its stored
  record is `passed=True 10/10`; only `gemma4:e2b` currently reports red.
- Dropdown filtering: `provider_registry.py:118` hardcodes
  `["gemma4:latest", "gemma4:12b", "llama3.1:8b"]` and the live merge comes
  from the **Ollama daemon**, which is stopped. Fix the defect; **bring the
  menu redesign to Stephen before implementing it** — his explicit instruction.
- Conclusion delivery in text chat (design agreed, unbuilt).

---

## Standing rules learned the hard way today

- **"Committed" is not "working."** Three separate times a fix was reported
  before it ran in the live process. Verify in the running server.
- **Suspect our plumbing before the model.** Twice proven: a broken gate
  harness, then `json.loads()` on an already-parsed dict dropping every tool
  argument. 0/5 became 15/15.
- **A key missing from `DEFAULT_SETTINGS` is deleted on every save.** Hit three
  times: `preferred_model`, `heavy_hitter`, `away_drain_after_s`.
- **Ask before restarting** when Stephen is testing.
