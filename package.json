{
  "name": "friday-desktop",
  "version": "5.5.0",
  "description": "Playwright end-to-end and visual test harness for Agent Friday. The application itself is Python; this file exists only to pin the test tooling.",
  "private": true,
  "scripts": {
    "test:app": "playwright test --config=playwright.app.config.ts",
    "test:smoke": "playwright test --config=playwright.app.config.ts smoke.spec.ts seams.spec.ts",
    "test:vision": "playwright test --config=playwright.app.config.ts vision.spec.ts",
    "test": "playwright test --config=playwright.app.config.ts smoke.spec.ts seams.spec.ts"
  },
  "keywords": [],
  "author": "FutureSpeak.AI",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.59.1"
  }
}
