# Orchestrator seat conformance runs

FR-1 from `dev/friday-orchestrator-integrity-spec.md`. Generated by
`npm run conformance:check -- <model>` against a locally running Ollama
instance, using the real production tool registry
(`src/main/desktop-tools.ts`'s `DESKTOP_TOOL_DECLARATIONS`).

- `2026-08-12-gemma3-4b-RED.txt` — gemma3:4b, the model seated as orchestrator
  during the 2026-08-12 incident. Ollama reports no `tools` capability for
  this model, and rejects every tool-bearing request with
  `"does not support tools"` (HTTP 400). 0/10. This is why it fabricated
  tool-call syntax instead of executing anything — not a serving-config bug,
  the model itself cannot use the structured tool-call channel. gemma3:4b
  remains fine for non-tool seats (summarization, tool-free voice chat).

- `2026-08-12-gemma4-latest-GREEN.txt` — gemma4:latest (8B), already pulled
  locally. Ollama reports `tools` capability, and it passes all 10 canned
  prompts on the structured channel with zero prose leakage. 10/10.

Re-run any time the orchestration model changes: `npm run conformance:check
-- <model>`. A red run should not hold the orchestrator seat.
